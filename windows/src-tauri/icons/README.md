# Icons

> 这里的 PNG / ICO 是占位资源(纯色方块),可以正常构建。
>
> 真正发布前请用任意图标生成器替换为正式素材。推荐:
>
> ```bash
> npx @tauri-apps/cli icon path/to/source.png
> ```
>
> 这条命令会生成全尺寸的 `32x32.png / 128x128.png / 128x128@2x.png / icon.ico / icon.icns`。
